# =================================================================
# Project A.N.C. Environment Variables
# =================================================================
# Copy this file to .env and fill in your actual values.
# Do not commit the .env file to version control.

# =================================================================
# 1. CORE API CONFIGURATION
# =================================================================

# --- Chat API Configuration (v3.2.0+) ---
# IMPORTANT: Alice Chat now uses an API client architecture.
# The Chat API server handles all LLM calls, prompt construction, and RAG.

# Base URL of the Chat API server (without endpoint path)
CHAT_API_BASE_URL=http://localhost:8000

# API Key for authentication (optional, if your Chat API requires it)
COMPASS_API_KEY=your_api_key_here

# --- Legacy API Provider Selection (Deprecated) ---
# NOTE: These settings are no longer used in v3.2.0+
# The Chat API server handles provider selection internally.
# Kept for backwards compatibility only.
# CHAT_API_PROVIDER=google
# GEMINI_API_KEY=your_api_key_here
# OPENAI_API_KEY=your_api_key_here


# =================================================================
# 2. MODEL SELECTION (Optional)
# =================================================================
# Override the default models specified in config/config.py.

# --- Remote AI Models (for Alice Chat via API Server) ---
# The model specified here will be sent to the API server via config.model
# and will be used for AI responses.
# Example for Gemini: gemini-2.0-flash-exp, gemini-2.5-pro, gemini-2.5-flash
# Example for OpenAI: gpt-4o, gpt-4-turbo
GEMINI_MODEL=gemini-2.5-pro
OPENAI_MODEL_NAME=gpt-4o

# --- Alice Chat Parameters ---
# Temperature for model randomness (0.0-2.0, default: 1.0)
# Lower = more focused/deterministic, Higher = more creative/random
ALICE_TEMPERATURE=1.0

# --- Local AI Models (for AI Analysis Plugins) ---
# Requires Ollama to be running.
# Download models with `ollama pull <model_name>`
OLLAMA_MODEL=gemma3:4b
SENTIMENT_COMPASS_MODEL=gemma3:4b


# =================================================================
# 3. ALICE CHAT BEHAVIOR
# =================================================================

# --- Conversation History ---
# The number of characters of recent conversation to include as context.
ALICE_HISTORY_CHAR_LIMIT=4000

# The maximum number of messages to keep in Alice's short-term memory.
MAX_HISTORY_LENGTH=500

# --- Conversation Persistence ---
# How often to automatically save the conversation state (in seconds).
AUTO_SAVE_INTERVAL=30


# =================================================================
# 4. COMPASS API (PAST CONVERSATION SEARCH)
# =================================================================
# Connects to the compass-api service for retrieving relevant past conversations.

# --- Connection ---
COMPASS_API_BASE_URL=http://127.0.0.1:8000

# --- Search Strategy ---
# "search": Standard vector similarity search.
# "graph_search": Graph search that includes related memories.
COMPASS_API_ENDPOINT=search

# --- Search Behavior ---
# "latest": Use only the most recent user message for the search query.
# "history": Use the entire recent conversation history for the query.
COMPASS_API_SEARCH_MODE=latest

# The data field to target for the search.
# Options: "content", "summary"
COMPASS_API_TARGET=content

# --- Result Limits ---
# The number of initial results to fetch from the search.
COMPASS_API_LIMIT=5

# The number of related items to fetch per result (for "graph_search" only).
COMPASS_API_RELATED_LIMIT=3

# --- Advanced ---
# Enable/disable AI-powered response compression from the API.
# Use "true", "1", or "yes" for True; anything else for False.
COMPASS_API_COMPRESS=False

# --- Authentication (Optional) ---
# If your compass-api requires authentication, provide the API key here.
COMPASS_API_KEY=your_compass_api_key_here


# =================================================================
# 5. PATH OVERRIDES (Optional)
# =================================================================
# Override default data paths.

DATA_DIR_NAME=data
NOTES_DIR_NAME=notes
ARCHIVE_DIR_NAME=.archive
DB_FILE_NAME=anc_db.json
MEMORIES_DIR_NAME=memories
NIPPO_DIR_NAME=nippo
PROMPTS_DIR_NAME=prompts
CHAT_LOGS_DIR_NAME=chat_logs

ALICE_SYSTEM_PROMPT_FILE=0-System-Prompt.md
ALICE_MEMORY_FILE=0-Memory.md
CREATE_MEMORY_PROMPT_FILE=create_memory_prompt.md
CREATE_NIPPO_PROMPT_FILE=create_nippo_prompt.md


# =================================================================
# 6. DEBUGGING AND ADVANCED SETTINGS
# =================================================================

# --- Debug Mode ---
# Set to "true", "1", or "yes" to enable verbose debug logging.
ANC_DEBUG=false
